projection;
strict ( 2 );
use draft;

define behavior for yzrap_p_cds_demo alias demo_booking
{
  use create;
  use update;
  use delete;


  // Action Button
  use action actionStatus;
  use action actionInactive;

  // For Draft-6
  use  action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}